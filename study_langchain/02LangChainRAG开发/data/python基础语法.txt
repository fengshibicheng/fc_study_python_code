#====================Python基础语法====================
#1.注释
#单行注释用#
"""
多行注释，三双引号
可以换行
"""
'''三单引号也是多行注释'''

#2.变量和常用数据类型
a = 10          #int 整型
b = 3.14        #float 浮点型
c = "python"    #str 字符串
d = True        #bool 布尔(True/False)
e = [1,2,3]     #list 列表(可变)
f = (1,2,3)     #tuple 元组(不可变)
g = {"name":"小明","age":18} #dict字典

#查看数据类型 type()
print(type(a))

#3.输入输出
#输出
print("Hello Python",a)
#输入 input默认字符串
name = input("请输入姓名：")
num = int(input("请输入数字：")) #转为整型

#4.运算符
#算术：+ - * / // % **
# / 浮点除法 //整除 %取余 **幂运算
#比较：> < >= <= == != 结果True/False
#逻辑：and or not

#5.if条件语句（缩进控制代码块，默认4空格）
score = 80
if score >= 90:
    print("优秀")
elif score >= 60:
    print("及格")
else:
    print("不及格")

#6.循环
#for循环 range(左闭右开)
for i in range(1,6):
    print(i)

#遍历列表
lst = [10,20,30]
for item in lst:
    print(item)

#while循环
i = 1
while i <=5:
    print(i)
    i +=1
#break跳出循环 continue跳过本次循环

#7.字符串操作
s = "abc123"
print(s[0])     #下标取值
print(s[1:3])   #切片
print(len(s))   #获取字符串长度
s_new = s + "test" #字符串拼接

#8.列表增删改查
arr = [1,2,3]
arr.append(4)       #末尾追加元素
arr.remove(2)       #删除指定元素
arr[0] = 99         #修改下标对应值
print(arr[1])       #索引取值

#9.自定义函数 def
def add(x,y):
    return x+y

res = add(3,5)
print(res)

#10.常用关键字
#pass占位空代码、global全局变量、in成员判断